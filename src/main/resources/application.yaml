server:
  port: 8088
spring:
  datasource:
    url: jdbc:postgresql://localhost/booktracker_db?currentSchema=booklist
    username: postgres
    password: sqlpostgre
    driver-class-name: org.postgresql.Driver
  sql:
    init:
      mode: always
      continue-on-error: false
#      data-locations: classpath:demo-data.sql
security:
  jwt:
    secret: ZWZqb3B3ZWpmd2plY2Zwd2plY2Zwb2p3ZWNmb2x3ZWNmcG93 #while secret is not change - all users keys stay valid
    access: 3600000 #1 hour
    refresh: 2592000000 #30 days